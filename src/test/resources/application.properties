# Intentionally left empty. Test-specific properties moved to application-test.properties (activated by @ActiveProfiles("test")).
# Previously a stray server.port=8081 was here which caused duplicate application config origins during tests.
